# Implementation Plans

Generated by the improve skill on 2026-06-30. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

Based on commit `90eb38429098d91d870af5bc05a926723893ae14`. Run drift check
before starting any plan: `git diff --stat 90eb384..HEAD -- <plan's in-scope paths>`.

## Execution order & status

| Plan | Title                                                   | Priority | Effort | Depends on      | Status                            |
| ---- | ------------------------------------------------------- | -------- | ------ | --------------- | --------------------------------- |
| 001  | Fix release workflow binary names (`smt` → `licencify`) | P1       | M      | —               | DONE (`b6e7c13`)                  |
| 002  | Fix Apache-2.0 license detection false positive         | P1       | S      | —               | DONE (`44bd08b`)                  |
| 003  | Add `cargo audit` to CI and justfile                    | P2       | S      | 001 (same file) | DONE (`b6e7c13`, merged with 001) |
| 004  | Add integration tests for CLI commands                  | P2       | L      | —               | SKIPPED (user request)            |
| 005  | Write project README                                    | P2       | S      | —               | DONE (`7c6d6b4`)                  |

## Dependency notes

- 003 touches the same CI file as 001; they can be done in either order but
  should not conflict — step on the same file sequentially (001 first, then
  003 rebases on its changes) or combine into one branch.

## Findings considered and rejected

- _Subdir-prefix matching ambiguity_ (`config.rs:246-257`): On closer reading,
  the prefix check correctly requires the path separator after the prefix,
  so `src/foobar` won't match `src/foo`. Not a bug, not worth changing.
- _`Runner` trait over-engineering_: The trait enables testable git author
  resolution. For a CLI that only shells out to `git config`, it's just enough
  abstraction. No finding here.
- _Missing license templates_ (BSL-1.0, Zlib, etc.): Not a bug — the SPDX API
  fills the gap. A nice-to-have enhancement, not an audit finding.
